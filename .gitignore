# Official GitHub reference for ignoring files:
# https://help.github.com/articles/ignoring-files/

# Node.js dependencies and package managers
/node_modules
/.pnp
.pnp.js
.yarn/*
!/.yarn/patches # Keep patches if used
!/.yarn/plugins # Keep custom Yarn plugins
!/.yarn/releases # Keep Yarn releases
!/.yarn/sdks # Keep SDKs if you're using them
!/.yarn/versions # Keep Yarn version files

# Next.js build output
/.next/
/out/

# Production build output
/build

# Logs (npm, Yarn, and others)
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/logs
*.log

# Coverage reports from testing
/coverage

# OS-specific files
.DS_Store
Thumbs.db

# SSL certificates and keys
*.pem

# Local environment variables
.env
.env.*     # Exclude all .env files (use .env.example for defaults)
!.env.example # Keep .env.example to specify required env vars

# Editor/IDE settings
.vscode/*
.idea/*

# macOS/Linux-specific files
.AppleDouble
.LSOverride

# Typescript build artifacts
*.tsbuildinfo
next-env.d.ts

# Miscellaneous temporary files
*.swp
*.swo
*.bak

# Vercel deployment
.vercel

# Optional: include rules to ensure proper commit structure
.gitlab-ci.yml
docker-compose.yml

# Ignore lockfiles to avoid conflicts across dev environments
package-lock.json
yarn.lock